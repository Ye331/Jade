# Jade Spirit High-Resolution Frames

Drop final transparent PNG animation frames here using the `JadeSpirit_*` names from `Assets/Design/JadeSpirit_AnimationImportSpec.md`.

Then run:

`Jade > Build Jade Spirit Player Assets`

If this folder is empty, the builder falls back to the current 8-frame jade spirit prototype sprites in `JadeQilinFrames`.
